#include "kruskal.h"

namespace graphs {
std::vector<graphs::Edge> graphs::kruskalSkeleton(std::vector<std::vector<std::pair<int, int> > > &graph)
{
    std::vector<int> used;
    used.resize(graph.size());

    for(const auto& vertex: graph)
    {

    }
}
}

